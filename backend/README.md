# PharmaTree backend

This directory contains the PharmaTree Solidity contract, Hardhat
configuration, deployment scripts, and regression tests.

From this directory:

```bash
npm install
npm run compile
npm test
npm run node
```

For the complete local setup, Sepolia deployment instructions, environment
configuration, and security guidance, see the repository
[README](../README.md).
