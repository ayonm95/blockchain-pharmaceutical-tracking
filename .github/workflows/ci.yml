name: PharmaTree CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test-contracts:
    name: Smart Contract Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: backend/package-lock.json

      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci

      - name: Compile Solidity contracts
        working-directory: ./backend
        run: npx hardhat compile

      - name: Run Hardhat unit tests
        working-directory: ./backend
        run: npx hardhat test

  build-frontend:
    name: Frontend Build & Typecheck
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build Next.js application
        working-directory: ./frontend
        env:
          NEXT_PUBLIC_PHARMA_TREE_CONTRACT: "0x36ea40cb4edc7e304DA1A280480afd16ABb124D0"
          NEXT_PUBLIC_RPC_URL: "https://sepolia.infura.io/v3/ab602f75684b462da53b56b8e765e5a2"
          NEXT_PUBLIC_CHAIN_ID: "11155111"
          NEXT_PUBLIC_DEPLOYMENT_BLOCK: "11671278"
        run: npm run build
